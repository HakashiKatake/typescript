const superHeroes: string[] = []
//const heroPower: number[] = []
const heroPower: Array<number> = []

type User = {
    name: string,
    isActive: boolean
}

const allUsers: User[] = []

const MLModels: number[][] = [
    [255, 0, 0],
    [0, 255, 0],
    [0, 0, 255],
    //'[255, 255, 0]' // This is a string, not a number array
]





superHeroes.push("Iron Man")
heroPower.push(100)

allUsers.push({name: "Saurabh", isActive: true})

